name: "Node Setup"
description: "Setup Node.js with npm caching"

runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: 22
        cache: npm
        cache-dependency-path: test-track-fe/package-lock.json

    - name: Install dependencies
      shell: bash
      run: |
        cd test-track-fe
        npm ci
